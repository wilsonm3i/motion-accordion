{
  "name": "docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter motion-accordion build",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "pnpm --filter motion-accordion build",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@base-ui/react": "^1.2.0",
    "motion-accordion": "workspace:*",
    "react": "^19.2.4",
    "react-colorful": "^5.6.1",
    "react-dom": "^19.2.4"
  }
}
